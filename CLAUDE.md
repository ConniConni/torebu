# torebu（トレ部）

仲間と筋トレを記録・応援しあうアプリ「トレ部」。部活・筋トレ仲間などクローズドなグループ内で、
記録にリアクション／コメントし合いながらランキングで競い合う、交流特化のトレーニング記録アプリ。

**Claudeが実装を主導するプロジェクト。** ユーザーは仕様の判断と受け入れを担当し、
実装の詳細をすべて理解することは求めない。作り進めることを優先する。

> 当初は技術の学習を兼ねる方針だったが、MVP完成時点でコードとドキュメントの量が
> 学習の入り口として大きくなりすぎたため、2026-09-04に方針を変更した。
> 技術の学習は、規模の小さい別プロジェクトで行う。

## 技術スタック

- フロントエンド: Nuxt.js + TypeScript
- バックエンド: Express + PostgreSQL（Prisma推奨）
- 認証: セッション方式（JWTではない。理由は`docs/schema.md`参照）
- リアルタイム通知: Socket.io（Phase2で採用検討）
- DBホスティング: **Neon**（本番）／**Docker Compose**（ローカル開発）
  - Supabase（Auth/Storage/Realtimeがバンドル）ではなくNeon（素のPostgres）を選んだ理由：
    1. セッション管理・API設計を自前のExpressで実装する学習方針と合わせるため。バンドル機能に頼ると学習目的からズレる
    2. Supabaseの無料プロジェクトは1週間アクセスが無いと自動停止し手動再開が必要。断続的に触る個人学習プロジェクトだと地味にストレスになる。Neonは接続時に自動復帰するため放置に強い
  - Phase4で画像ストレージが必要になったら、Neon（DB）とは別にCloudflare R2等を組み合わせる想定（Supabase Storageだけ部分利用してもよい）

## リポジトリ構成（モノレポ）

```
torebu/
  frontend/   -- Nuxt.js
  backend/    -- Express + PostgreSQL
  docs/       -- 設計ドキュメント
```

## ドキュメント

- `docs/spec.md` — **実装済みの確定仕様（現状の正）。まずここを読む。**
- `docs/concept.md` — ブレストのサマリー（コア体験・機能アイデア・ターゲット・アプリ名の検討経緯）
- `docs/schema.md` — DBスキーマのドラフト（フェーズごとのテーブル定義・設計方針・セキュリティ実装の優先度）
- `docs/screens.md` — MVPの画面フロー・画面一覧
- `docs/roadmap.md` — Issueバックログ（着手予定のIssueをタイトルだけ並べた見通し用のメモ）
- `docs/learning-log.md` — 学習ログ（**MVPまでの記録。凍結済みで、以降は更新しない**）

より詳細な検討経緯（比較・トレードオフの議論など）は、企画メモ（Claudeとのセッションで作成したArtifact）にまとまっている。

## 開発の進め方

- 機能はMVP → Phase2 → Phase3 → Phase4の順で育てる（詳細は`docs/concept.md`のフェーズ一覧）
- 新しい依存関係を追加するときは、なぜ必要かを一言添える
- **実装したら`docs/spec.md`を同じPRで更新する**（詳細は「開発フロー」5）

## 出力前のセルフチェック（Claudeへの指示）

ユーザーは提案内容を自力で評価しない前提で進める。**Claudeが実装を主導する方針にした分、
ここがコード品質を守る唯一の関門になる。** そのため、
コード・設計提案・ドキュメントなど**まとまった内容を提示する前に、必ず一度自分で見直してから出す**。

- コード：意図通りに動くか、明らかなバグ・考慮漏れがないか、プロジェクトの規約に沿っているかを確認する
- 設計・提案：`CLAUDE.md`・`docs/`の既存の決定事項と矛盾していないかを確認する
- セルフチェックは品質を上げる工程であって、完璧の保証ではない。見直しても間違いはあり得る前提で、指摘されたら素直に直す

## 開発フロー（Claudeへの指示）

変更の経緯を後から追えるようにし、問題があれば戻せるようにするため、
mainへ直接コミットせず以下の流れをデフォルトの振る舞いとする。
細かいルールは`docs/git-workflow.md`参照。ユーザーから明示的に「今回はスキップ」と言われた場合はそれに従う。

1. **新しいまとまった作業（MVPロードマップの1項目など）に着手する前に、Issueの下書きを提案する**
   - `.github/ISSUE_TEMPLATE/task.md`のフォーマットで、背景・やること・完了条件を埋めて提示する
   - 作業が小さすぎる／自明な場合（typo修正など）はIssue化を提案しなくてよい
   - **下書きに合意が取れたら、Claudeが`gh issue create`で起票する**（起票前に本文を提示して確認を取る）
2. **実装に入る前に、ブランチ名を提案する**（`docs/git-workflow.md`の命名規則に沿う）
3. **実装中は、コミットを意味のある単位で区切ることを意識する**（粒度の目安は`docs/git-workflow.md`参照）。コミット自体はClaudeが行ってよいが、pushは必ずユーザーに確認する
4. **作業がまとまったら、PRの下書きを提案する**（`.github/PULL_REQUEST_TEMPLATE.md`のフォーマット）
   - あわせて簡単なセルフレビューをする：コミット粒度・命名規則・実装内容に気になる点があれば先に指摘する
   - **合意が取れたら、Claudeが`gh pr create`で作成する**（ドラフトにはしない）。マージはユーザーが行う
5. **仕様が変わる実装をしたら、`docs/spec.md`を同じPRの中で更新する**（最重要）
   - `docs/spec.md`は「実装済みの確定仕様」を書く唯一の場所であり、**次のセッションのClaudeが現状を把握する入口**でもある
   - ユーザーは実装の詳細を検証しない前提のため、ここが実装から遅れると、Claude自身が誤った前提で次の実装を始め、それを誰も検知できない状態になる
   - 更新対象：API一覧（§4）・データモデル（§5）・画面一覧とフロー（§3）・決めたこと（§2）。
     画面をまたぐ状態の持ち方（§3-3）を増やしたときは必ず追記する
   - 実装を変えたのにspec.mdを変えていないPRは、原則として未完成とみなす
6. **1 Issue = 1セッションを目安にする**。PRをマージしたら`/clear`か新規セッションで次のIssueに入る。デバッグで行き詰まって同じところを繰り返している時も、わかったことをメモしてから仕切り直すとよい

## テスト方針

- ツールはVitestに統一（フロント・バック共通）。API結合テストはSupertest、E2Eは後回しでPlaywright候補
  - Vitestを選んだ理由：
    1. Nuxt 3は内部でVite（Nuxt自体がVite製）を使っているため、Vitestとの相性が良く追加設定が少ない。Jestだと、ESM/TypeScriptとの相性でbabel等の変換設定が余計に必要になりやすい
    2. Jestとほぼ同じ書き方（`describe`/`it`/`expect`）なので、Jestの情報・チュートリアルもほぼそのまま参考にできる。乗り換えコストが低い
    3. フロント（Nuxt）専用ではなく、バックエンド（Express）のテストにもそのまま使える。**モノレポ全体で1つのテストツールに統一できる**ため、学習コストが1つに集約できる
    4. Nuxt公式が`@nuxt/test-utils`というVitest向けの統合モジュールを提供しており、公式サポートがある
- 優先順位：①バックエンドの認可・権限ロジック（高） ②ランキング集計等のロジック系（中） ③API結合テスト（中） ④主要フローのE2E（中、Phase2以降） ⑤フロントのコンポーネント単体（低、今は見送り）
- テストDBは本物のPostgreSQL（`jsonb`・部分UNIQUE制約などSQLiteでは代用しにくい機能を使うため）
- **テストはClaudeが実装と同じPRの中で書く。** 上の優先順位に沿って、認可・権限まわりは必ずテストを付ける
