# バックログ（保留事項・課題・アイデア）

> 実装済みだが未対応・未決定の事柄をここに集約する。**「今どうなっているか」は[spec.md](./spec.md)、
> 「未対応事項」はこのファイル**、という役割分担（このファイルを新設した経緯は
> [Issue #34](https://github.com/ConniConni/torebu/issues/34)参照）。
>
> 種別ごとに分けているが、厳密な分類より「後で拾えること」を優先する。項目には可能な限り
> **再検討のタイミング**を添える。Issue化する価値が出たら`roadmap.md`の「次のIssue候補」に上げる。

## ドキュメントと実装のズレ

MVP完成後の棚卸しで見つかった、「決めたはずなのに入っていない」もの。判断がブレる原因になる。

- **パスワードリセット**：`users`に`password_reset_token` / `password_reset_expires_at`カラムは
  あるが、APIは未実装

## 技術的負債

- **`prisma`（CLI, devDependencies）の依存に脆弱性**：`@prisma/config`経由で`deepmerge-ts`にhigh脆弱性
  あり（2026-09時点）。`@prisma/client`（実行時に使う方）はこの依存を持たず、`prisma`CLIも開発時にしか
  使わないため実害は低いと判断し対応保留。修正には`prisma@6.12.0`への破壊的ダウングレードが必要な
  ため、Prisma側のアップデートで解消されるのを待つ（`npm audit`で定期的に状況確認）
- **`GET /workouts`が日付範囲での絞り込みに未対応**：常に自分の全workoutを返す（Issue9実装時点）。
  ②ホーム画面のカレンダーは当面この全件取得結果をクライアント側で表示中の月だけ使う形で凌いでいる
  （APIへのリクエスト自体は絞り込んでいない）。記録件数が増えるとレスポンスサイズ・取得時間が
  伸び続ける設計のため、いずれ`performedAt`の範囲指定クエリパラメータをAPI側に追加する必要がある
  - **再検討のタイミング**：実際に一覧取得が重いと感じるようになったタイミング。それまでは
    件数が少なく実害が出にくいため、無理に先取りで実装しない
- **ログイン試行時、バックエンドのログに`express-rate-limit`の`ValidationError`
  （`ERR_ERL_UNEXPECTED_X_FORWARDED_FOR`）が出る**（2026-09-05、ローカル開発中に発見）。
  [auth.ts](../backend/src/routes/auth.ts)の`loginRateLimiter`（IPベースの総当たり対策）が、
  `X-Forwarded-For`ヘッダは付いているのにExpress側の`trust proxy`が未設定（デフォルト`false`）な
  ことを検知して警告している。ローカル開発ではNuxt開発サーバ（Nitro）がAPI呼び出しをバックエンドへ
  中継する際にこのヘッダを付与しているのが原因。例外ではなく警告のみでログイン自体は失敗しないため
  実害は無いが、`trust proxy`が未設定のままだと`req.ip`は常にNuxt側のIPになり、実クライアントIP単位の
  レート制限になっていない
  - **再検討のタイミング**：本番でリバースプロキシを挟む構成になり、環境ごとに`trust proxy`を
    正しく設定する必要が出たタイミング。ローカルでは信頼すべきでないヘッダのため、今`trust proxy`を
    有効にするのは逆にリスクがあり、先取りしない

## UI改善アイデア（2026-09-04の画面探索で見つかったもの）

> MVP完成後、実際に画面を一通り触って洗い出した気づき。既存の項目と重なるものは統合済み

- ~~**③記録作成の「中断してホームへ」というボタン名が、セット編集・削除の追加で紛らわしくなった**~~
  → [Issue #56](https://github.com/ConniConni/torebu/issues/56)で「ホームへ戻る」に変更し対応済み
  （2026-09-04）
- **③記録作成で同じ種目を選び直すと、同名の種目カードが2枚（記録済み表示＋新規入力欄）並ぶ**：どの
  セットがどちらのカードに属すか一見分かりにくい
- ~~**③→④種目選択（`/workouts/exercises`）から戻ると、開いていた`?date=`が失われ今日の記録に
  切り替わってしまう**~~ → [Issue #65](https://github.com/ConniConni/torebu/issues/65)で対応済み
  （2026-09-04）。③の「＋種目を追加」ボタンの遷移に`returnTo: \`/workouts/new?date=${targetDate}\``を
  クエリへ積み、④・⑦（`/workouts/exercises-new`）が戻り先へ`?date=`を引き継ぐようにした。なお
  「＋ルーティンから選ぶ」はページ遷移を伴わない画面内ピッカーのため対象外
- **ルーティン適用時、全種目が重複除外された場合に無反応に見える**：「0件追加されました」のような
  フィードバックが無く、ピッカーが閉じるだけ
- **セット入力欄（「＋種目を追加」で開く、新規セット用の重量・回数欄）に値を入れたまま「記録」を
  押さずに「ホームへ戻る」を押すと、その入力値は保存されずに消える**（ユーザー指摘）。入力途中の値も
  保存する方がUXは良さそうだが、「記録」を押すたびに1件確定保存する現在の設計思想（決めたこと2-6）
  と噛み合わない部分があり、変更範囲の見極めが必要
  - **[Issue #67](https://github.com/ConniConni/torebu/issues/67)（2026-09-04）でメモ・記録済み
    セットの編集（重量・回数）を明示的な保存ボタンからblur自動保存に変更したことで、この項目だけ
    「記録」を押さない限り保存されない、という非対称がより目立つようになった。ただしこの項目は
    「未確定の新規入力をどう扱うか」（自動で1件保存する／何もしない、のどちらにしても決めたこと2-6
    の見直しを伴う）という別種の設計判断が必要なため、Issue #67のスコープには含めず、この項目として
    残す
  - **再検討のタイミング**：③の新規セット入力（「記録」ボタン）自体の入力方式を見直すタイミング
    （下記「セット記録画面：同じ種目で複数セット〜」の項目とも関係する）
- ~~**③記録作成ですでに記録済みの種目にさらにセットを追加したい場合、「＋種目を追加」から同じ種目を
  選び直す必要がある**~~ → [Issue #69](https://github.com/ConniConni/torebu/issues/69)で対応済み
  （2026-09-04）。記録済みのセットカードに直接「＋セット追加」ボタンを付け、④種目選択を経由せず
  その場でセット入力欄を開けるようにした（詳細は`docs/spec.md`§3参照）
- ~~**③「＋種目を追加」で種目を選んだだけでセットを1つも保存せずにホームへ戻ると、②ホームの
  カードに「種目未登録」というリンクが残る**~~ → [Issue #63](https://github.com/ConniConni/torebu/issues/63)
  で、workoutの作成タイミングを③開始時点から「最初のセット記録・メモ保存のいずれか」に遅らせる
  ことで対応済み（2026-09-04）。何も保存しなければworkout行自体が作られないため、②に残ることは
  なくなった
- **メモだけ保存して種目を1つも記録せずに完了した場合も、②ホームには「種目未登録」と表示される**
  （[Issue #63](https://github.com/ConniConni/torebu/issues/63)のPRレビューで指摘、2026-09-04）。
  Issue #63でworkoutの作成タイミングを遅らせたことで、種目を1つも追加していない状態でも「メモを保存」
  すればworkout行が実在するようになった。この場合は実データがあるため「削除」「完了」ボタンが
  出るのは正しい一方、②側の表示が「種目未登録」のままだと、意図してメモだけ残した記録なのか
  種目登録を忘れた記録なのかが区別しにくい。表示文言・見せ方を再検討する
  - **再検討のタイミング**：②ホームの記録カード表示を見直すタイミング
- **セット表示で重量が自重（`weightKg === null`）のとき、「自重」の文字数と数値+kg表記とで
  横幅がずれて見える**（2026-09-04、ホーム②の一覧をセット内訳表示にした際の指摘）。③記録作成・
  ⑥記録詳細・②ホームの3箇所で同じ表示パターン（`weightKg ?? '自重'` を固定幅で右寄せ）を使い回して
  いるため、直すなら3箇所まとめての対応になり影響範囲がそれなりにある。今回は先送り
- ③記録作成の詳細UI（数字入力の方式、＋セット追加のインタラクション、自重トグル）
- セット記録画面：同じ種目で複数セットを連続して行う際、1セットごとに「記録」ボタンを押す方式に
  している（Issue10実装時点）。ジムで実際に使う場面では、複数セット分をまとめて入力してから
  一括登録できた方が手間が少ない可能性があるため、入力方式の改善を今後の課題として検討する
  - **再検討のタイミング**：実際に使ってみて「1セットごとの記録が手間」という体感が積み重なった
    タイミング。それまでは動作の分かりやすさ（都度保存され消えない）を優先し、無理に先取りしない
- **②ホームのカレンダーで、記録がある日についている印（点）が見づらい**（PRレビューで指摘、2026-09-04）。
  現状は[HomeCalendar.vue](../frontend/app/components/HomeCalendar.vue)で日付の下に小さな点を表示する
  だけ（`hasRecord`フラグ）だが、もう少し視認性の高い表現（日付を○で塗りつぶす等）に変更を検討する
- ②ホームの「＋今日の記録を始める」ボタンの文言を「＋今日の記録をつける」に変更する案（Issue10の
  レビューで出た指摘）。優先度が低いため見送り、まとめて見直すタイミングで対応する
- ④「もっと見る」をアイコン付きの開閉ボタンにする等の見た目の装飾。優先度が低いため見送り、まとめて
  見た目を整えるタイミングで対応する
- ③のセット記録画面から④種目選択へ直接戻れるリンクを追加する案。「この種目の入力を終える」→
  「＋種目を追加」の2手順で代替可能なため優先度は低いが、動線として欲しくなったら追加する
- ③「今日の記録を完了」を押したときに確認を挟む案（元のIssue10には無かったが、レビューで「押しても
  反応が無いように見える」という指摘から検討したアイデア）。`feat/workout-recording`ブランチでは
  `window.confirm()`を使ったところ、ブラウザが「このページに追加のダイアログを表示させない」を
  自動的に有効化すると以後`confirm()`が常に`false`を返し、ボタンが完全に無反応に見える不具合を
  実機で確認した。**今後この手の確認UIを作るときは`window.confirm()`を避け、画面内の2段階確認
  （キャンセル／実行ボタン）にする**、という学びだけ残しておく（実装例は同ブランチ参照）
- ~~**③記録作成で、種目が1つも記録されていない状態でも「この記録を削除」ボタンが押せてしまう**~~
  → [Issue #63](https://github.com/ConniConni/torebu/issues/63)で対応済み（2026-09-04）。上記の
  workout遅延作成により、何も保存していない間は`workoutId`が無い状態になるため、「この記録を削除」
  セクション自体を表示しないようにした（削除するものが実際に無い状態では出さない、という方向で解消）
- ~~**③記録作成の「今日の記録を完了」ボタンが、見た目上「ホームへ戻る」と同じ挙動に見える／
  「メモを保存」「セット編集の保存」ボタンも不要かもしれない**~~ →
  [Issue #67](https://github.com/ConniConni/torebu/issues/67)で対応済み（2026-09-04）。メモ・
  セット編集（重量・回数）はどちらも入力欄からのblurで自動保存に変更し、明示的な「保存」ボタンを
  廃止。「今日の記録を完了」と「ホームへ戻る」は実質同じ操作（②ホームのキャッシュ再取得→遷移）
  だったため1つの「ホームへ戻る」に統合した（詳細は`docs/spec.md`§3-2参照）

## 未決定事項

- **`exercises.default_sort_order`の運用**：当面すべて`null`で運用し、値を設定する手段（管理API・
  シードスクリプトいずれも）は用意しない（Issue5で検討）。結果、MVP期間中は実質「使用回数→名前順」の
  2段階ソートとして機能する。運営（自分）が公式種目を追加する際に「誰が・どう定番順を設定するか」の
  運用ごと決める必要があり、専用の管理APIを作るなら「運営かどうか」を区別する認可の概念（Userにrole
  相当のフィールド等）を新設するコストも伴う。値が最後まで使われないなら、カラム自体を削除する
  選択肢もある
  - **再検討のタイミング**：公式種目のシード投入・追加を実際に行うタイミング（種目データをある程度
    まとめて用意する必要が出た時）。それまでは判断材料（実際に定番順を固定表示したいニーズが本当に
    あるか）が揃わないため、無理に今結論を出さない
- **⑤ルーティン関連（一覧`/routines`→編集`/routines/[id]`）の画面遷移が、他の画面と毛色が違う**
  （ユーザー指摘、2026-09-04）。他の画面はできるだけ画面遷移を減らす方向で作ってきた
  （例：③記録作成はセット追加・編集・削除・記録削除まで1画面で完結し、⑥記録詳細も統合して
  廃止した。④⑦も③に「戻ってくる」導線としてreturnToで設計している）のに対し、⑤だけは
  「一覧→別画面の編集ページに遷移して名前変更・種目の追加/削除/並び替えをする」という、
  一覧からの明示的な画面遷移を必要とする作りのまま。この非対称が使い勝手として妥当か、
  ③のように1画面（またはモーダル等）に寄せる方が良いか、そもそもルーティンをどういう場面で
  ユーザーに使ってもらいたいか（作成頻度は低いはずなので画面遷移があっても問題ないのか等）
  から再検討する
  - **再検討のタイミング**：③⑥統合がひと段落し、次の画面まわりの改善に着手するタイミング
- ~~**⑤ルーティン編集の「保存」ボタンを廃止し、自動保存に統一する**~~ →
  [Issue #72](https://github.com/ConniConni/torebu/issues/72)で対応済み（2026-09-04）。
  当初「名前欄を変更しても保存ボタンが非活性のまま」というバグ報告として調査していたが、実際には
  バグではなく誤解だったと判明（[routines/\[id\].vue](../frontend/app/pages/routines/[id].vue)の
  種目追加/削除/並び替えは既に即時保存で、保存ボタンがあるのは名前欄のみ。種目追加時の
  `watch(routine, ...)`のdeep watchで`nameInput`表示が巻き戻り、あたかも種目追加で保存ボタンの
  状態が変わったように見えたことが誤解の原因と推測）。③（メモ・セット編集）が既にblur時自動保存に
  統一済み（[Issue #67](https://github.com/ConniConni/torebu/issues/67)）なのに⑤の名前欄だけ
  明示的な保存ボタンが残っているのは非対称なため、名前欄も他画面と同様にblur時自動保存へ統一した
- **`routine_exercises`に目安のセット数・重量・回数を持たせ、ルーティン適用時に実際の`workout_sets`
  として即登録できるようにする**（ユーザー指摘・2026-09-04再検討）。Issue7では「セットごとに値が
  異なる」ことを理由にスコープ外としていたが、体感（毎回同じ重量・回数を打ち直すのが面倒）が
  出たため再検討した。詳細は今回のセッションのやり取り参照
  - **決めた方向性**：完全なワンクリックは狙わない。適用時に目安値をそのまま`workout_sets`として
    登録し、実際と違った分だけ既存のセット編集UI（blur自動保存）で手直しする方式にする。修正UIを
    新設せず既存フローを流用できるため、手直しのコストはほぼゼロにできる見込み
  - **スキーマ**：目安セット（重量・回数の配列）を持たせるには子テーブルを新設するかjsonbカラムを
    足すかの2択。`exercises.highlight_slugs`（`docs/schema.md`）でjsonb配列を使う前例があり、
    件数が少なく個別行への外部参照も不要なデータのため、`routine_exercises.target_sets jsonb null`
    のようなjsonbカラム案の方がコストが低いと判断（子テーブルほど重い変更にはならない見込み）
  - **本体のコスト**：スキーマよりも、③のルーティン適用フロー（`usePendingExercises`、現状は
    「入力待ちの種目を積むだけ、値は毎回手入力」前提）を「即登録＋手直し」方式に作り直す部分が
    本体のコストになる
  - **規模の見立て**：1 Issueには収まらなそうなため、着手時は以下の2段階に分けることを推奨
    - [x] Issue A：スキーマ（jsonbカラム追加）＋バックエンドAPI＋⑤編集画面への目安セット入力UI
          （[Issue #74](https://github.com/ConniConni/torebu/issues/74)、2026-09-05対応済み）
    - Issue B：③のルーティン適用フローを「即登録＋手直し」方式に作り直す
  - **再検討のタイミング**：Issue Bは`roadmap.md`「次のIssue候補」に記載
- **過去日からの記録作成**：③のページ自体は[Issue #52](https://github.com/ConniConni/torebu/issues/52)で
  `?date=`クエリに対応済み（`docs/spec.md`§3-4参照）。②ホーム側の「選択した過去日にまだ記録が無い
  場合、その日の記録作成を始める」導線も[Issue #61](https://github.com/ConniConni/torebu/issues/61)で
  実装済み（`docs/spec.md`§2表10行目・§3-2参照）。**解消済み**
- **②ホームのカレンダーで日付を選択したときの挙動**：2026-09-04時点の最終判断は「**日付選択自体の
  挙動（クリックで下に一覧表示）は変更しない**」（[roadmap.md](./roadmap.md)「③記録作成・⑥記録詳細の
  統合」補足、ユーザー判断）。
  - 同日中に一度「日付クリック→③記録作成画面へ直接遷移する」設計へ変更する方向で検討したことが
    あったが、③⑥統合が完了した時点で「一覧表示のまま変更しない」に確定し直した。過去の記録は
    一覧表示された各記録のリンク（`/workouts/new?date=...`）から編集する導線のみとする
  - モバイルでの「選択した日を左右スワイプで前日／翌日に切り替える」というアイデアも出たが
    （2026-09-04）、②のカレンダー・日付一覧表示に閉じた操作にする（③記録作成画面側にスワイプ機能を
    持たせない）方向で一致。**今回は実装を見送り**、必要になったら改めてIssue化する
  - **再検討のタイミング**：スワイプ操作が必要になったとき
- ~~**③種目を1つも追加していない状態で「今日の記録を完了」を無効化するガードの要否**~~ →
  [Issue #63](https://github.com/ConniConni/torebu/issues/63)のworkout遅延作成により解消（2026-09-04）。
  何も保存していない間は「今日の記録を完了」ボタン自体を表示しない（完了するものがまだ無いため）
  形にした。ガードで無効化するのではなく、そもそも出さない方向で対応
- **Phase2以降の未着手画面**：グループ関連画面（作成・招待・メンバー一覧・削除）、通知一覧画面、
  ランキング画面、「イチオシこだわり共有」投稿・一覧画面。設計の方向性は
  [concept.md](./concept.md)・[schema.md](./schema.md)に記載済みだが、画面単位の詳細設計は未着手
  - **再検討のタイミング**：MVPがひと段落し、Phase2に着手するタイミング
- **同じルーティン名を複数作成できてしまう**（ユーザー指摘、2026-09-05・Issue #74の作業中に発見）。
  `POST /routines`のバリデーションは`name`の長さ・空文字チェックのみで、一意性は見ていない
  （DB側にもUNIQUE制約なし）。⑤一覧・③「＋ルーティンから選ぶ」ピッカーは`createdAt`降順で名前しか
  出さないため、同名が並ぶと見分けが付きにくくなる
  - 個人用のリストなので実害は「間違えて別のを選ぶ」程度に留まり、種目マスタの重複対策で
    DB制約を見送った判断（`docs/schema.md`「重複・表記ゆれ対策」）と近い軸で考えられそう
  - 対応するとしても選択肢は複数ある：①何もしない（実害が小さいため） ②作成/変更時にアプリ側で
    重複を警告する（種目名の部分一致サジェストと同じ方向性） ③DB制約（`UNIQUE (user_id, name)`等）
    を追加する
  - **再検討のタイミング**：実際に同名ルーティンで選び間違えた／使い勝手として気になったタイミング

## 先送りにした学習項目

- **セッションストアの自前実装**（`sessions`テーブルへの読み書き）＋期限切れセッションを掃除する
  定期実行ジョブ。Issue4で一度検討したが、`connect-pg-simple`に乗る判断をしたため見送り（経緯は
  [schema.md](./schema.md)の設計方針メモ参照）。定期実行ジョブの学習を兼ねて、いずれ別Issueとして
  着手する
- **CI導入**（GitHub Actionsで、PR作成時にVitest・typecheck・lintを自動実行）＋テストDBの分離
  （Issue4時点ではテストが開発用DBに直接アクセスする簡易構成で開始。API一式（Issue4〜7）が揃うあたりで、
  CIと合わせてテスト専用DBの分離にも着手する想定）
